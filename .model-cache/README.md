# Optional build cache

Place `fast-bge-small-zh-v1.5.tar.gz` in this directory to build the container
without downloading the embedding model. The archive is intentionally not
committed; Docker falls back to the official download URL when it is absent.
