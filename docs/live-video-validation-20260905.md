# 2026-09-05 真实试片验收

授权范围：1–3 个 4 秒、非 2K 视频。本轮实际创建 **1 个**视频供应商任务，参数为
`seconds=4`、`resolution=768P`。未创建第二或第三个试片，也未触发付费重新生成。

## 实际结果

- 项目：`67d44765-840b-464d-8ebc-683162db9e8c`。
- 创意：雨后清晨，成年女性在窗边说“雨停了，出发吧。”。
- 通过真实用户 API 创建、持久化队列执行；分镜确认绑定显示方案的摘要。
- 故事、分镜和生成前检查完成后，明确确认分镜才调用视频供应商。
- 单个镜头保留 H3 原生音轨，按对白时间窗烧录字幕，再进入最终合片和技术质检。
- 实测视频轨：H.264，768 × 1344，4.000 秒；音轨：AAC，约 4.011 秒。
  MP4 容器总时长约 4.021 秒，包含封装/音频帧时长差异。
- 技术报告 `passed=true`、`scope=technical_integrity`，无技术问题。
- 发布了独立 worker；数据库迁移至 `20260905_0019`；数据库、Redis、Pi Runtime 就绪。
  重启后作品仍为 completed，队列没有剩余执行任务。

## 真实运行发现并修复的问题

旧数据库保留的 `ck_director_project_status` 不允许 `awaiting_storyboard`，导致文本规划
成功后无法进入用户确认阶段。之前仅按 ORM 建表的测试遗漏了这个历史约束。

新增 `0019` 迁移并补齐 ORM 约束。新增数据库测试重建旧约束，验证升级允许分镜确认，
回退将待确认项目保留为 `awaiting_confirmation`，不会转为付费制作队列。
本次恢复同一个项目，复用了已有故事、分镜和生成前检查结果；失败期间的视频调用数为零。

## 验证

- Python：126 项通过；9 项数据库测试在普通运行中按配置跳过。
- 隔离 PGlite 数据库：上述 9 项全部通过，含状态约束升级/回退。
- Node：5 项 Pi Runtime 和 4 项前端测试通过。
- Ruff 与 Git diff 空白检查通过。
- 付费验收脚本覆盖超出时长、分辨率、镜头数量和不确定创建结果的停止规则。

## 用户评价

已向用户展示成片并请求 1–5 分评价、具体修改意见和长期偏好。
截至这份记录，尚未收到用户评价，项目反馈为空，未写入虚构评分或长期偏好。
后续通过作品反馈 API 保存用户实际意见；只有明确的可复用偏好进入后续创作记忆。

技术检查不验证对白逐字准确率、口型、字幕实际同步、人物一致性或审美满意度。
本次仅证明单镜头短试片的主流程可运行，不代表长视频与多镜头连续性已经验收。
